extends base

prepend window_title

  = page.title
  &mdash;

append content
    article.container(class=page.layout)
        h1=page.title
        div.entry
            include _partial/article_meta

            +meta(page)

            != page.content

            include _partial/article_pagination